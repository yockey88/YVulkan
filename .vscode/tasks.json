{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Machy-1.0.0 Windows Build",
            "command": "${workspaceFolder}/build.bat",
            "group": {
                "kind": "build",
                "isDefault": true,
            }
        }
    ]
}